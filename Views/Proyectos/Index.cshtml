@using FrontendApi.Models

@model List<Proyecto>

@section Styles
    {
    <link href="~/datatables.net-bs5/datatables.bootstrap5.min.css" rel="stylesheet" />

    }

<div class="container">
    <h1 class="text-center">Proyectos</h1>

        <a class="btn btn-primary my-3" asp-controller="Proyectos" asp-action="CreateUpdateProyectosView" asp-route-id="0">
        Agregar curso
    </a>

    <table id="tableId" class="table ">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Descripción</th>
                <th scope="col">Fecha</th>
                <th scope="col">Editar</th>
                <th scope="col">Detalle</th>

            </tr>
        </thead>
        <tbody>
             @foreach (var proyecto in Model){
                <tr>
                    <th scope="row">@proyecto.IdProyecto</th>
                    <td>@proyecto.Descripción</td>
                    <td>@proyecto.Fecha.ToString("dd/MM/yyyy")</td>
     
                    <td>
                        <a asp-controller="Proyectos" asp-action="CreateUpdateProyectosView" asp-route-id="@proyecto.IdProyecto" class="btn btn-warning">Editar</a>
                    </td>
                    <td>
                        <a asp-action="DetalleProyecto" asp-route-id="@proyecto.IdProyecto" class="btn btn-info">Detalle</a>
                    </td>
                </tr>
              }
            
        </tbody>
    </table>
</div>


@section Scripts
    {
    <script src="~/lib/datatables.net/jquery.datatables.min.js"></script>
    <script src="~/datatables.net-bs5/datatables.bootstrap5.js"></script>
    <script src="~/datatables.net-bs5/datatables.bootstrap5.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#tableId').DataTable({
                "paging": true,
                "searching": true,
                "ordering": true,
                "info": true,
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.13.6/i18n/es-ES.json',
                },
            });
        });
    </script>
    }
                    

